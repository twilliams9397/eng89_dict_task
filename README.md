# This is the code for the dictionary task
```python
story1 = {
    "start": "the beginning of the story",
    "middle": "the middle section of the story",
    "end": "the conclusion of the story"
}

print(story1)
print(type(story1))
print(story1.keys())
print(story1.values())
print(story1["start"])
print(story1["middle"])
print(story1["end"])

story1["hero"] = "Batman" # adding key: value pair
```